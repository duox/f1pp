`#if` directive group parsing test.

This test virtually contains several sub-tests that validate preprocessor behaviour in parsing of following directives:
- `#ifdef/ifndef` directives
- `#else` directive
- `#endif` directive

